[package]
name = "hi-log-core"
version = "0.1.0"
edition = "2021"
description = "hi-log 核心库:大文件加载、稀疏行索引、检索、标记(与 UI 无关)"

[dependencies]
memmap2 = "0.9"
memchr = "2"
regex = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
encoding_rs = "0.8"

[dev-dependencies]
tempfile = "3"

